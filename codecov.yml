codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: 1
    patch:
      default:
        target: 0

github_checks:
  annotations: false

ignore:
  - '**/tests/**'
